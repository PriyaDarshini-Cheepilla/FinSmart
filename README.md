
  # FinSmart Mobile App UI

  This is a code bundle for FinSmart Mobile App UI. The original project is available at https://www.figma.com/design/uW47r0GaMAegnIMXKWasvw/FinSmart-Mobile-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  